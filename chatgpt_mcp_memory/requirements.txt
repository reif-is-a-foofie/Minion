# Core: storage + embeddings + MCP. Required for every install.
numpy>=1.26.0,<2.0.0
# fastembed bundles ONNX models + onnxruntime; avoids torch/transformers entirely.
fastembed>=0.4,<0.8
tqdm>=4.66
ollama>=0.3.3
# SQLite-vec gives us vector KNN without a separate service.
sqlite-vec>=0.1.6
# Watchdog powers the inbox auto-ingester.
watchdog>=4.0
# FastAPI powers the desktop UI / Tauri sidecar.
fastapi>=0.115
uvicorn>=0.30
# uvicorn needs an explicit WebSocket protocol impl; without it, /events 404s silently.
websockets>=12.0
pydantic>=2.7
# Word documents (.docx)
python-docx>=1.1.0
# PDF + HTML/text extraction (desktop bundle and default MCP install — no extra pip step)
trafilatura>=1.12
pypdf>=4.3
pdfminer.six>=20231228
markdown-it-py>=3.0
